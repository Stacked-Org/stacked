import 'package:stacked_core/stacked_core.dart';

@StackedApp(
  routes: [],
  logger: StackedLogger(
    disableReleaseConsoleOutput: true,
  ),
)
class App {
  /** This class has no puporse besides housing the annotation that generates the required functionality **/
}
